# lituus-lab

This is an experimentation lab. We use it to stage and publish applications
and libraries written in [Nim](https://nim-lang.org/).

The workspace holds several works in progress; each one moves into its own
repository as it matures. Nothing here is a released product yet — consider
everything a draft until it lands in its own repo.

More details will show up here as the work takes shape.
